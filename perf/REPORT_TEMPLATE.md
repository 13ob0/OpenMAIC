# OpenMAIC 性能测试报告（模板）

**环境**: 内网 `BASE_URL`: ___________________  
**日期**: ___________________  
**执行人**: ___________________  
**版本**: （来自 `/api/health` 的 `version`）___________________

---

## 1. 摘要

- **结论**（应用单机容量 / 上游瓶颈 / 待跟进）：

---

## 2. 范围与假设

| 项 | 说明 |
|----|------|
| 压测类型 | 如：仅 health / health + chat SSE |
| 是否调用真实 LLM | 是 / 否；若「是」，Key 与模型：________ |
| 部署形态 | 如：`next start` 单机 / Docker / 其他：________ |

---

## 3. 场景与参数

| 场景 | 脚本 | 主要参数 | 起止时间 |
|------|------|----------|----------|
| 阶梯负载 | `perf/k6/health.js` | `SCENARIO=load` | |
| 尖峰 | `perf/k6/health.js` | `SCENARIO=spike` | |
| 浸泡 | `perf/k6/health.js` | `SCENARIO=soak`, `SOAK_DURATION=...` | |
| Chat SSE | `perf/k6/chat-stream.js` | `CHAT_MAX_VUS`, `CHAT_SHARED_ITERATIONS` | |

---

## 4. 结果摘要（k6）

### 4.1 `/api/health`

| 指标 | 结果 | 阈值（脚本默认） | 是否通过 |
|------|------|------------------|----------|
| http_req_failed | | &lt; 0.1% | |
| p95 延迟 | | p(95)&lt;500ms | |
| p99 延迟 | | p(99)&lt;1000ms | |

### 4.2 `/api/chat`（若执行）

| 指标 | 结果 | 说明 |
|------|------|------|
| http_req_failed | | |
| p95 http_req_waiting（TTFB） | | 受上游 LLM 主导 |
| p95 http_req_duration | | 完整流时长 |
| chat_errors | | |

---

## 5. 服务端观测（若采集）

| 时间窗口 | CPU 峰值 | 内存趋势 | FD / 异常 | 备注 |
|----------|----------|----------|-----------|------|
| | | | | |

---

## 6. 瓶颈归因

- **应用（Node/Next/代理）** 证据：

- **上游 LLM / 媒体** 证据：（如 429、超时、与 health 表现不一致）

- **其他**（磁盘、网络、配置）：

---

## 7. 建议与后续

1. 
2. 

---

## 8. 附件

- [ ] k6 原始输出 / JSON 导出路径：________
- [ ] 服务端采样日志路径：________
